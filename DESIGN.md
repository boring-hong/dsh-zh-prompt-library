# DESIGN.md — 增强按钮的呼吸动效与皮肤配色

> 依据 `boujoy-code-web-design`（DSH Cordis Client UI 设计）产出。
> 适用范围：`dsh-prompt-enhancer` 的浏览器半按钮（`lib/client.js`），以及后续同类输入框控件。
> 状态：**待确认后落地**。按 skill 纪律，规范先行、代码随后。

## 1. 视觉主题与氛围

**一句定调**：安静的工具栏控件 —— 静止时几乎退到背景里，只在真正干活时"呼吸"。

**氛围关键词**：克制、有生命感、不抢注意力。

**交互档位**：**L2 流畅交互**（L1 静态 + 状态过渡动画）。
理由：这个按钮钉在输入框工具行里，旁边就是发送键和用户正在打的字。L3 的滚动驱动/光标跟随在这里
是纯干扰；而"检索中"必须有一个不靠文字也能感知的状态，所以保留一层动效。

## 2. 色彩系统（CSS 变量，三套皮肤）

所有颜色走 `--enh-*` 变量，组件里**不出现硬编码色值**，因此加皮肤只需加一组变量。

| 变量 | 作用 | 绿（默认·流萤） | 蓝 | 粉 |
|---|---|---|---|---|
| `--enh-accent` | 主色：文字、边框、发光 | `#7ee787` | `#6cb6ff` | `#f778ba` |
| `--enh-accent-strong` | 强调态（更高对比，用于 focus 环） | `#a4f0aa` | `#9ecbff` | `#ff9ecb` |
| `--enh-fill` | 静止底色 | `rgba(126,231,135,.10)` | `rgba(108,182,255,.10)` | `rgba(247,120,186,.10)` |
| `--enh-fill-hover` | hover 底色 | `rgba(126,231,135,.17)` | `rgba(108,182,255,.17)` | `rgba(247,120,186,.17)` |
| `--enh-fill-active` | 工作中底色（渐变落点） | `rgba(126,231,135,.20)` | `rgba(108,182,255,.20)` | `rgba(247,120,186,.20)` |
| `--enh-border` | 静止边框 | `rgba(126,231,135,.30)` | `rgba(108,182,255,.30)` | `rgba(247,120,186,.30)` |
| `--enh-border-hover` | hover 边框 | `rgba(126,231,135,.55)` | `rgba(108,182,255,.55)` | `rgba(247,120,186,.55)` |
| `--enh-glow` | 呼吸光晕（`box-shadow` 用） | `rgba(126,231,135,.35)` | `rgba(108,182,255,.35)` | `rgba(247,120,186,.35)` |
| `--enh-text-dim` | 辅助文字 | `var(--dsw-alias-label-tertiary)` | 同左 | 同左 |
| `--enh-text-error` | 错误态 | `#f85149` | 同左 | 同左 |

**皮肤改动只有一个变量名**：把 `--enh-accent` 及其派生值换一组即可。切换方式见第 5 节（`skin` 选项）。

配色选择依据：
- **绿**：与既有的流萤主题同调，是当前默认，改动为零成本。
- **蓝**：GitHub Primer 的 `#58a6ff` 一族的降饱和版。适合"工具/系统操作"语义，最不挑主题。
- **粉**：GitHub Primer 的 `#f778ba`。适合轻量、创作向的场景。

刻意**不用紫色渐变**（skill 红线：「无紫色渐变/圆角卡片/Inter 字体组合」）。

## 3. 动效规范

### 3.1 时长与缓动（遵循 skill 的 fast/normal/slow 与缓动约定）

| 交互 | 时长 | 缓动 |
|---|---|---|
| hover 色移 / 边框 | 140ms | `ease-out` |
| active 按压（`scale .97`） | 80ms | `ease-out` |
| 状态文字出现 | 200ms | `ease-out` |
| **呼吸循环** | **2800ms** | `cubic-bezier(.45, .05, .55, .95)`（近正弦，两端慢、中间快） |

呼吸用 2800ms 而非更快：这是一段"背景活动"，不是提示。周期低于 1.5s 会变成闪烁，
高于 4s 又感觉不到；2.8s 落在"能感知到在动、但不催人"的区间。

### 3.2 呼吸曲线

一个周期内只改三件事，且**全部走 opacity / box-shadow / background**，不触发布局：

| 相位 | 进度 | 不透明度 | 光晕 |
|---|---|---|---|
| 吸气起点 | 0% | .58 | `0 0 0 0` |
| 峰 | 50% | 1 | `0 0 9px var(--enh-glow)` |
| 回落到起点 | 100% | .58 | `0 0 0 0` |

`opacity` 峰值只到 1.0、谷值 .58 —— 谷值不低于 .5 是刻意的：低于 .5 会让按钮上的文字
在暗色主题下发灰、可读性下降。

### 3.3 触发时机

**只在 `busy === true`（检索+改写进行中）时呼吸。** 其余状态完全静止。

理由：按钮空闲时也呼吸，等于在用户打字时持续制造视觉噪声 —— 这是 skill 里"克制"要防的事。
真正需要动效的就是那 3–6 秒的等待，它让用户确认"在跑"而不是"卡住了"。

### 3.4 无障碍（`prefers-reduced-motion`）

```css
@media (prefers-reduced-motion: reduce) {
  .enh-btn-anim { animation: none !important; }
}
```

降级后**功能与语义不变**：动画带来的"在运行"信息由已有的状态文字承担（"改写中…"）。
即动效是**增强**，不是信息载体。

### 3.5 性能

- 只动 `opacity` / `box-shadow` / `background`，均为合成层属性，不触发 reflow。
- 动画只挂在按钮自身，不放到容器上，避免影响工具行其它控件。
- `will-change` **不加**：单个 26px 按钮的透明度动画不值得常驻提升合成层。

## 4. 组件四态（补齐 skill 要求的 hover/active/focus/disabled）

| 状态 | 表现 |
|---|---|
| 默认 | `--enh-fill` 底 + `--enh-border` 边 + `--enh-accent` 字 |
| hover | `--enh-fill-hover` 底 + `--enh-border-hover` 边 |
| active | `transform: scale(.97)`，80ms |
| **focus-visible** | `outline: 2px solid var(--enh-accent-strong)`，`outline-offset: 2px`（键盘可达性） |
| disabled / busy | `opacity: .75`（由动画接管） |

「撤销」相位（`data-phase="undo"`）用 `--enh-fill-active` 底，提示"再点会撤销"，与工作中区分。

## 5. 皮肤的实现与扩展

- 变量定义在 `:root`，皮肤类只覆盖变量：`.enh-skin-blue { --enh-accent: …; }`
- 组件接一个 `skin` 选项（`'green' | 'blue' | 'pink'`），默认 `'green'`。
- 加第四套皮肤 = 加一个类 + 一组变量，**不动任何组件逻辑**。

## 6. 自查（skill 的 100 分红线，逐项）

| 检查项 | 结论 |
|---|---|
| 不用 Inter/Roboto/Arial 默认字体 | ✅ 沿用已有 `font-family: inherit` |
| 完整色彩系统（≥8 变量） | ✅ 10 个 `--enh-*` 变量 × 3 套皮肤 |
| 亮/暗双模式或声明单模式原因 | ✅ 全部依赖主题 token，亮暗自动跟随 |
| 间距体系一致 | ✅ 仅沿用既有 9px/5px/6px，无新任意值 |
| 阴影体系有意义 | ✅ 仅一处发光，语义为"运行中" |
| 可交互元素四态齐备 | ✅ 默认/hover/active/**focus-visible**/disabled |
| 动效 ≤300ms 且有缓动 | ✅ 过渡 140/200ms；呼吸为状态动画并已做 reduced-motion 降级 |
| 空/加载/错误态 | ✅ 已有"改写中…"/`失败：…`/`先输入内容` |
| 自包含 | ✅ 一个 CSS 字符串，无外部依赖 |
| 无紫色渐变 + Inter | ✅ 无 |

**结论：满足红线，可落地。**
